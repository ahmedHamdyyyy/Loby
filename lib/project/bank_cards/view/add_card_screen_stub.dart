import 'package:flutter/material.dart';

/// Temporary stub to satisfy analyzer when bank card screens are commented out.
class AddCardScreenVendor extends StatelessWidget {
  const AddCardScreenVendor({super.key});

  @override
  Widget build(BuildContext context) {
    // This screen is intentionally empty as bank card flows are disabled/commented.
    return const SizedBox.shrink();
  }
}
